<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;1,400;1,500;1,700&display=swap" rel="stylesheet"> 
    <link rel='stylesheet' href='https://cdn-uicons.flaticon.com/uicons-regular-rounded/css/uicons-regular-rounded.css'>
    <link rel="stylesheet" href="/style/style.css">
    <title>Edit product</title>
</head>
<body>
    <div class="wrapper">
        <div class="section-add">
                    <div class="add-product">
                        <a href="/list-product"><i class="fi fi-rr-arrow-left icon-arrow-left"></i></a>
                        <h3 class="title-login">Cập nhật sản phẩm mới</h3>
                        <div class="form-group">
                            <label class="form-label">Tên sản phẩm</label>
                            <input data-name="" value="<%= product.productName %>" type="text" name="productName" class="form-input product-name" placeholder="Nhập tên sản phẩm">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Giá sản phẩm</label>
                            <input value="<%= product.productPrice %>" type="number" name="productPrice" class="form-input product-price"  placeholder="Nhập giá sản phẩm">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Ảnh sản phẩm</label>
                            <input  class="file-input form-input" type="file" id="upload-file" name="productImg" accept="image/*">
                            <label class="file" for="upload-file">
                                <!-- <span class="select-img active">Chọn ảnh</span> -->
                                <span class="show-text"><%= imgName %></span>
                                <label class = "file-browse" for="upload-file">Browse</label>
                            </label>
                            <p class="error-upload"></p>
                            <p class="show">
                                <img src="/<%= product.productImg %>" alt="">
                            </p>
                            <p class="show-img">
                            </p>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Mô tả</label>
                            <textarea class="form-input product-desc" name="productDesc" id="" placeholder="Nhập mô tả"><%= product.productDesc %></textarea>
                        </div>
                        <% if (locals.error) { %>
                            <div class="errow-message">
                                <%= error %>
                            </div>
                        <% } %>
                        <div class="form-btn">
                            <button data-id="<%= product.id %>"  data-name="<%= product.productName %>" data-price="<%= product.productPrice %>" data-desc="<%= product.productDesc %>" data-img="<%= product.productImg %>" type="submit" class="btn btn-update">Cập nhật</button>
                        </div>
                    </dib>
        </div>
    </div>
</body>

<script src="/js/main.js"></script>
</html>